# Sanitized report examples

These CSV files use synthetic metadata and relative paths. They demonstrate the
standard audit report format without exposing a real music library.

Generated reports belong in `reports/`, which is ignored by Git.
